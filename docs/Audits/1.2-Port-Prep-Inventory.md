# 1.2 Port-Prep Inventory (generated on the 1.1 code, 2026-05-31)

Worklists for [`1.2-DevEnv-Upgrade-Runbook.md`](1.2-DevEnv-Upgrade-Runbook.md) Part C, produced before
the env is up so the porting has concrete targets. Regenerate after the port to verify zero remaining.
(Numbers from a scan of `Source/**`; `Content/**` for assets.)

## A. API-surface map (recompile-risk surface)

**113 distinct FactoryGame headers** included; **100 distinct `AFG*/UFG*/FFG*` types** referenced.
The compile-fix checklist (Part C1) is dominated by a handful of types — fixing these first clears most
breaks. Most-referenced types (occurrences):

| refs | type | refs | type |
|---:|---|---:|---|
| 987 | `AFGHologram` | 95 | `AFGConveyorChainActor` |
| 554 | `UFGFactoryConnectionComponent` | 88 | `AFGBuildableFactory` |
| 482 | `AFGBuildable` | 88 | `AFGBuildablePowerPole` |
| 287 | `AFGBuildableConveyorBase` | 61 | `AFGSplineHologram` |
| 256 | `UFGPipeConnectionComponent` | 61 | `UFGPowerConnectionComponent` |
| 210 | `UFGRecipe` | 60 | `UFGItemDescriptor` |
| 152 | `AFGBuildablePipeline` | 51 | `AFGBuildableConveyorLift` |
| 126 | `AFGPlayerController` | 45 | `AFGBuildableWire` |
| 119 | `UFGPipeConnectionComponentBase` | 45 | `AFGCharacterPlayer` |
| 107 | `UFGCircuitConnectionComponent` | 42 | `AFGBuildableSubsystem` |

Top headers by including-TU count: `Hologram/FGHologram.h` (30), `FGPlayerController.h` (24),
`FGPipeConnectionComponent.h` (23), `FGRecipe.h` (21), `FGFactoryConnectionComponent.h` (20),
`Buildables/FGBuildable.h` (20). Cross-reference against the `v3.11.3...dev` compare for which moved.

## B. TObjectPtr migration worklist (Part C2)

Rule: **only `UPROPERTY` members** convert; locals/args stay raw. Found **201 raw-pointer `UPROPERTY`
members + 11 raw-pointer containers** (`TArray/TMap<U*>`). Heavily concentrated — and the top two are
UMG `BindWidget` properties (`UButton*`/`UTextBlock*`/etc.), a fully mechanical swap:

| count | file |
|---:|---|
| 105 | `Public/UI/SmartSettingsFormWidget.h` (UMG BindWidget) |
| 36 | `Public/UI/SmartUpgradePanel.h` (UMG BindWidget) |
| 14 | `Public/Subsystem/SFSubsystem.h` |
| 10 | `Public/Services/SFHintBarService.h` |
| 7 | `Public/Data/SFHologramData.h` |
| 7 | `Public/Features/Extend/SFExtendService.h` |
| 5 | `Public/HUD/SFHudWidget.h` |
| ~17 | spread across ~12 other service/feature headers (1–2 each) |

~141 of 201 are the two UI widgets (scriptable: `UPROPERTY(...) UType* X;` -> `UPROPERTY(...) TObjectPtr<UType> X;`).
The remaining ~60 are service back-pointers (`USFExtendService* Owner`, etc.) and `SFHologramData.h`
connection pointers — also mechanical. Force the check during the port with
`NonEngineNativePointerMemberBehavior=Disallow` in the UHT config and clear the list.

## C. Enhanced Input worklist (Part C3)

Small, contained surface:
- **1 Input Mapping Context:** `Content/SmartFoundations/Input/Contexts/MC_Smart_BuildGunBuild.uasset`
  — reparent to `UFGChildInputMappingContext`; it becomes a primary asset (auto-discovered only as a
  GFP); implement the `GetPrimaryAssetId()` workaround if SML doesn't ship it.
- **20 Input Actions:** `Content/SmartFoundations/Input/Actions/IA_Smart_*.uasset` — migrate the
  player-mappable settings (`Is Player Mappable`, Name, Display Name, Display Category) from the
  mapping-context "Mappings" records onto these actions.
- **Code (recover field values from the committed .uassets / 30.1.0 build, not the old editor):**
  `Input/SFInputRegistry.{h,cpp}` (registration), `Subsystem/SFInputHandler.{h,cpp}` (the input module),
  `Services/SFHintBarService.{h,cpp}`, `Subsystem/SFSubsystem*` — usages: `UFGInputMappingContext` (18),
  `UInputAction` (38), `EnhancedInput` (29), `BindAction` (26), `InputComponent` (40).
- Note: inputs may now require a C++ module to wire — we already have one (`SFInputHandler`).

## Dependency versions (exact, from the "Required Software" page)

- **Visual Studio 2022** (NOT 2026) + MSVC `v143 (v14.34-17.4)`, `.NET 6.0 Runtime`, `.NET Framework 4.8.1 SDK`,
  workloads "Desktop development with C++" + "Game development with C++". (Alpakit automation also targets
  **.NET 8** per `origin/dev` — install the .NET 8 SDK too; docs page may lag.)
- **Engine:** link GitHub<->Epic, then GitHub<->SML via `https://linker.ficsit.app/link`, then download
  4 files from `github.com/satisfactorymodding/UnrealEngine/releases/latest`:
  `UnrealEngine-CSS-Editor-Win64-{1,2,3}.bin` + `UnrealEngine-CSS-Editor-Win64.exe` (same folder, exact
  names), run the `.exe`. (404 on the repo = GitHub link not finished.) **Use the dev docs branch — EXP.**
- **Clang cross-compile toolchain** `v25 clang-18.1.0-based` (only for Linux dedicated-server builds).
- **Wwise `2023.1.14.8770`** via Audiokinetic Launcher (Major 2023.1): packages Authoring + SDK(C++);
  platforms Linux + Windows VS2019/VS2022/Game Core; deselect all plugins.
- **SMM** (optional for dev; needed to test installs).
